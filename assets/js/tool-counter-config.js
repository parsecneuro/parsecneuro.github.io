/* Set by setup-counters.py after the counter service has been deployed.
 * Keep the administrator key out of this file and out of your GitHub repository.
 * An empty endpoint leaves counting disabled, including in local previews.
 */
window.TOOL_COUNTER_CONFIG = Object.freeze({
  endpoint: '',
  siteOrigin: 'https://parsecneuro.github.io'
});
